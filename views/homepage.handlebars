{{#each posts as |post| }}
<div class="card">
    <div class="title">
        <h2>{{post.title}}</h2>
        <p>By {{post.user.name}} on {{post.data_created}}</p>
    </div>
    <div class="content">
        <p>
            {{post.content}}
        </p>
    </div>
</div>
{{/each}}